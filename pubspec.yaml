name: namer_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ">=2.15.0 <3.0.0"  # Specify the Dart SDK version range here

dependencies:
  flutter:
    sdk: flutter
  
  english_words: ^4.0.0
  provider: ^6.0.0
  flutter_native_splash: ^2.3.1
  firebase_core: ^2.15.0
  firebase_auth: ^4.7.2
  cloud_firestore: ^4.8.4
  charts_flutter: ^0.11.0
  google_nav_bar: ^5.0.6
  line_icons: ^2.0.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter_native_splash:
  color: "#72A9C4"
  color_dark: "#272727"
  image: assets/images/evalia_logo.png

flutter:
  uses-material-design: true
  assets:
    - assets/images/
